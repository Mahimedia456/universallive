# Phase 38 — Navigation + Global Components

The main navigation contract remains Home / Studio / LIVE / Activity / Profile. Secondary tools use explicit back navigation rather than creating duplicate bottom tabs.

Shared global UI includes page headers, action rows, status panels, metric tiles, loading, empty, error and success states. Active Live keeps navigation priority while a broadcast is running.
