# Phase 35 — Diagnostics

Shows current account/backend state, connection and scene counts, capture/publisher status, encoder/output telemetry, stream defaults and device-permission state.

`Run Health Check` calls the existing backend account, connections, notifications and history APIs. It never displays credentials or auth tokens.
